import { Title } from '@atoms';
import { SectionTitle } from '@templates';
import { t } from '@i18n';

const ResumePage = () => {
  return <SectionTitle>{t('student-center')}</SectionTitle>;
};

export default ResumePage;
